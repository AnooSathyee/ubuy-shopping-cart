import React, { Component } from "react";
import PropTypes from "prop-types";

export default class Product extends Component {
  render() {
    return (
      <div>
        <h3>This is the product page from ubuy!</h3>
      </div>
    );
  }
}
